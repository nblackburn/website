import { sizes } from '@styles/theme.css';
import { style } from '@vanilla-extract/css';

export const latestProject = style({
    position: 'relative'
});

export const title = style({
    margin: '0 0 2rem 0',
    fontSize: sizes.medium
});
